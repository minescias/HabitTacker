#ifndef __STRINGS_REPLACE_H
#define __STRINGS_REPLACE_H

#include <string>

namespace Strings
{

std::string replaceAllCopy(const std::string& source,
    const std::string& from, const std::string& to);

} // Strings

#endif
